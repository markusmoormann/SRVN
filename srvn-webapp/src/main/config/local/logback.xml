<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="logback/local-default-appenders.xml"/>

    <include resource="logback/local-default-loggers.xml"/>

    <root level="warn">
        <appender-ref ref="root"/>
    </root>
</configuration>